
  # Multi-step Sample Pack Form

  This is a code bundle for Multi-step Sample Pack Form. The original project is available at https://www.figma.com/design/h58VGnGLkV799mFPgoskKf/Multi-step-Sample-Pack-Form.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  